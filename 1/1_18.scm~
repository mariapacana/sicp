(define (double x)
  (* x 2))

(define (halve x)
  (/ x 2))

(define (iter-helper a x y)
  (display x)
  (display ",")
  (display y)
  (newline)
  (cond ((= y 0) a)
        ((even? y) (mult (double x) (* (- 2 (/ x))
        (else (mult)))) 

(display (mult 3 100))